%This function is used to calculate the entropy of a dataset, 
function [entropy_true, entropy_false] = calculate_entropy(binary_labels)

positives = 0;
negatives = 0;

positives = length(find(binary_labels==1));
negatives = length(binary_labels) - positives;

if positives == 0 || 